# CPU-Scheduling
The project will result in a functional simulation of multiple CPU scheduling algorithms that demonstrates how processes are executed under different strategies. By running the same set of processes through FCFS, SJF, and Round Robin, the system will clearly show differences in execution order, waiting time, and turnaround time. 
